# הנחיות עבודה בפרויקט הזה

## זרימת העבודה שהמשתמש הגדיר

### 1. תיקיית «תשובות»

**כל תשובה שאני נותן נשמרת כקובץ טקסט בתיקיית `תשובות/`** - בלי יוצא מן הכלל,
גם כשמדובר בתשובה קצרה או בשאלת הבהרה.

שם הקובץ: `NNN - YYYY-MM-DD - <תיאור קצר>.txt`
כאשר NNN הוא מספר רץ תלת-ספרתי לפי סדר כרונולוגי.

לפני כתיבת קובץ חדש: לבדוק את המספר האחרון בתיקייה ולהמשיך ממנו.
לא לדרוס קבצים קיימים.

התוכן הוא התשובה עצמה בטקסט רגיל, קריא בלי עיצוב מיוחד.

### 2. תיקיית «שינויים במערכת»

- `שינויים במערכת/דברים לשנות.txt` - המשתמש כותב כאן מה לשנות.
- `שינויים במערכת/היה/` - ארכיון הבקשות שכבר בוצעו.

**כשהמשתמש אומר "רוץ"** (או כל וריאציה: "תרוץ", "בצע", "לך"):

1. לקרוא את `דברים לשנות.txt`.
2. להתעלם משורות שמתחילות ב-`#`.
3. אם משהו לא ברור או שיש לו כמה פירושים - **לשאול לפני שנוגעים בקוד**.
4. לבצע את השינויים.
5. להריץ `make test` ולוודא שהכל עובר. אם משנים נתונים - גם `make`.
6. להעביר את הקובץ ל-`היה/` בשם `YYYY-MM-DD - NNN - <תיאור>.txt`,
   עם סיכום קצר בראשו: מה התבקש, מה בוצע, ומה לא בוצע ולמה.
7. ליצור `דברים לשנות.txt` **ריק לחלוטין** - בלי כותרת, בלי הסבר,
   בלי תבנית. קובץ באפס בתים. זו בקשה מפורשת של המשתמש.
8. לשמור את התשובה ב-`תשובות/`.

אם בקשה מסוימת חוסמת או בעייתית - לבצע את כל השאר במלואו, ולומר במפורש
מה הושאר ולמה. לא לצמצם את ההיקף בשקט.

---

## כללי הפרויקט

**מספרי גיוס לעולם לא נכתבים ידנית בקוד.** כל יחס המרה, זמן גיוס והתפלגות
נשאבים מקובצי האקסל שבתיקיית «נתונים למערכת» דרך `tools/build_dataset.py`.
אם ערך צריך להשתנות - מחליפים את קובץ המקור ומריצים `make`. אין לערוך ידנית
את `data/recruitment_data.json`, את `index.html` או את `מחשבון גיוס.html` -
שלושתם נוצרים אוטומטית.

**עורכים את `web/template.html`, לא את קובצי הפלט.**
`index.html` הוא מה ש-GitHub Pages מגיש, ו-`מחשבון גיוס.html` הוא אותו תוכן
בדיוק כקובץ עצמאי לשליחה. שניהם תוצרי בנייה.

**שני מנועי חישוב חייבים להישאר מסונכרנים.** יש לוגיקת חישוב גם ב-
`recruit_calc/engine.py` וגם ב-JS שבתוך `web/template.html`, בין הסימנים
`__ENGINE_START__` ו-`__ENGINE_END__`. כל שינוי בלוגיקה חייב להיעשות בשניהם,
ו-`tests/test_parity.py` מוודא שהם מחזירים אותו דבר על מאות תרחישים.

**לא לנחש נתונים שאינם במקור.** שלב בלי מקור מסומן `has_data: false`, לא
מקבל יחס המרה, ואינו משתתף בחישוב. **כרגע לכל השלבים יש מקור** - «הגשות»
קיבל אחד ב-2026-09-02 (קובץ «הגשות חציון א 2026»). הכלל עצמו נאכף
ב-`tests/test_engine.py` על שלב מלאכותי (`blank_engine`), ולא על «הגשות»,
כדי שיישאר בתוקף גם כשלכל השלב האמיתיים יש נתונים. אין להחזיר בדיקות
שנשענות על «הגשות» כשלב חסר.

**שלושה קובצי פעילות, טבלה אחת.** `sources.activities.files` מונה שלושה
קבצים שמכסים תקופות חופפות. הם מאוחדים, ושורה כפולה - אותו מועמד, אותה
פעילות, אותו תאריך - נספרת פעם אחת. עמודה שאינה קיימת בקובץ מסוים
(«תוצאה» חסרה ב«כלל פעולות») נשארת ריקה בשורותיו ואינה מפילה את הבנייה.

**שלב יכול להיות ממופה לכמה סוגי פעילות.** `activity_type` הוא מחרוזת או
רשימה. «הגשות» מורכב משלוש צורות של הקמת מועמדות, ומועמד נספר לפי המוקדם
שבהן. `activity_types()` ב-`build_dataset.py` הוא המקום היחיד שמנרמל את זה.

**«כמה צריך שיעמדו שם» ו«אם הם שם היום» הם שני מספרים שונים, ושניהם
מוצגים.** `manager_plan` מחזיר את שניהם: הראשון הוא היעד חלקי שיעור הגיוס
של השלב, בלי לחץ זמן, והמחיר שלו הוא `deadline_days` = הזמן עד היעד פחות
חציון הימים עד הגיוס. השני הוא היעד חלקי `effective_rate`, כלומר הכמות
הדרושה ממי שכבר עומד בשלב עכשיו. הצגת אחד מהם בלבד מטעה: הראשון לבדו
מסתיר את הדד-ליין, והשני לבדו נראה כדרישה בלתי אפשרית. `manager_plan`
בנוי מעל `gap_plan` ואינו משכפל את חישוב הדרישה.

**טבלה אחת, לא טבלה לכל שלב.** `combined_matrix` מחזיר שורה לכל שלב על
פני חלונות הזמן. הפיזור (`spread`) מעוגל על הסכום הרץ, ולכן **סכום התאים
בשורה שווה בדיוק לסך הכול** - טבלה שאינה מסתכמת נראית כמו טעות.
`test_the_windows_always_add_up_to_the_row_total` אוכף את זה, ו-
`test_the_matrix_and_the_single_row_graph_agree` אוכף שהיא לא תתפצל
מ-`combined_when`.

**כיסוי ושיעור מעבר הם שני מספרים שונים, ואסור לבלבל ביניהם.** שיעור
המעבר אומר מה קורה למי שנמצא בשלב. `hire_coverage` אומר כמה מהגיוסים
בכלל עוברים שם. **רק כ-51% מהמגויסים נרשמו בכלל כהגשה**, ולכן 35,711
הגשות מניבות לפי השיעור כ-1,145 גיוסים בעוד שהארגון גייס באותה תקופה
2,328. הפער אינו טעות בשיעור אלא כיסוי חסר: כמחצית מהמגויסים מגיעים
בדרך שאינה יוצרת רשומת «הקמת מועמדות». המשתמש תפס את זה, ובלי
האזהרה המספר נראה כמו באג.

`covered` מנטרל קטיעה משמאל (מי שהתגייס בינואר 2026 הגיש ב-2025) על ידי
ספירה רק של מגויסים שהחלון שלפניהם מכוסה. שלב מתחת ל-`coverage_warning_below`
מקבל אזהרה מפורשת. `test_submissions_do_not_cover_the_whole_hire_flow`
נועל את הממצא - אם קובץ עתידי יכסה את כל הגיוסים היא תיפול ותאלץ להסיר
את האזהרה במקום להשאיר אותה כשהיא כבר לא נכונה.

**כל השיעורים מודדים הגעה, לא מעבר.** «61.2% מהגשות לבדיקת קבצים»
פירושו שבוצעה להם בדיקה, לא שהם עברו אותה. **לבדיקת קבצים אין בכלל
עמודת תוצאה בקבצים** - 0 מתוך 33,254 שורות - ולכן אי אפשר להפריד בין
מי שעבר לבין מי שנכשל, ואין להציג את המספר כשיעור מעבר. הניסוח בתצוגה
הוא «מגיעים» ולא «עוברים», ואין להחזיר אותו.

**`from_first` אינו מכפלה של `from_prev`.** במשפך המלא (`build_funnel`)
כל אחת משתי עמודות המעבר נמדדת ישירות בין שני השלבים שלה.
`test_from_first_is_not_a_product_of_the_step_rates` אוכף שהן לא יזדהו.
מאותה סיבה, «מועמדים ייחודיים» אינו יורד בהכרח משלב לשלב.

**כל מועמד משויך לערוץ אחד.** הפילוח (`assign_segments`) לוקח את «דרישה»
מרשומת ההגשה של המועמד, ואם אין - מהפעילות המוקדמת ביותר שלו. לכן
הערוצים אינם חופפים וסכומם קטן מכלל המועמדים.
`test_segments_do_not_overlap_and_fit_inside_the_whole` אוכף את זה.

**גזירה קדימה בלבד.** מספר שהוזן בשלב מייצג קבוצה שנמצאת *עכשיו* באותו שלב.
ממנה נגזר רק מה שקורה הלאה. אין לגזור כמה מועמדים היו בשלבים מוקדמים יותר -
זה מידע שאין לנו. `tests/test_engine.py` אוכף את זה.

**תכנון מיעד אינו גזירה לאחור, והוא מותר.** השאלה "כמה צריך בשלב X כדי
לגייס Y" היא המטרה המרכזית של המערכת. היא אינה טענה על העבר של קבוצה קיימת
אלא דרישה קדימה. `plan_from_target` מחשב את הכמות הנדרשת ואז גוזר ממנה
קדימה בדיוק כמו `project_cohort`, כך שהזנת אותה כמות במחשבון הרגיל מחזירה
בדיוק את אותו משפך. אין לשבור את השוויון הזה - `tests/test_engine.py` אוכף
אותו.

**גרף אחד, ולא גרף לכל שלב שהוזן.** המשתמש ביקש במפורש ששני גרפי הזמן יהיו
מאוחדים: שורה אחת לכל שלב, שמסכמת את כל הקבוצות. הזמן בשורה מאוחדת הוא
ממוצע משוקלל של חציוני הימים לפי מספר המועמדים, ונאמר במפורש שהוא משוקלל.

**«כמה יתגייסו» ו«כמה יתגייסו עד תאריך» הם שני מספרים שונים.** כשהוזן
תאריך יעד בזרימה קדימה, המספר הגדול חייב לענות על מה שנשאל - כמה עד
התאריך - ולידו נאמר מהו המספר הכולל. `hire_curve` נמדדת יום ביום
מקובצי המקור ולא נגזרת מחלונות הזמן, כדי שתאריך באמצע חלון יקבל תשובה
מדודה ולא משוערת. אין להחליף אותה באינטרפולציה בין חלונות.

**תאריך לא תקין נפסל במפורש.** `new Date(2026, 8, 31)` מגלגל בשקט
ל-1 באוקטובר. `parseDay` מוודא שהרכיבים חזרו כמו שהוזנו, ואין להסיר
את הבדיקה הזו.

**תאריכים הם שכבת תצוגה בלבד.** המנוע מחזיר ימים. ההמרה לתאריך נעשית ב-JS
שבממשק וב-`cli.py`, מחוץ לסימני `__ENGINE_START__`, ולכן אינה כפופה לדרישת
התאימות בין שני המנועים.

**תצפית שנפסלה נפסלת בשלמותה.** תצפית שאינה התקדמות אמיתית במשפך אינה
נספרת לא בזמנים **ולא בשיעור ההגעה** - אין היגיון לפסול אותה בזמנים ולספור
אותה כהמרה מוצלחת.

**הרצפה נמצאת מצורת ההתפלגות, לא מסטיית תקן.** רישום שנעשה בדיעבד יוצר
בליטה בימים הראשונים, אחריה שקע, ואז האוכלוסייה האמיתית. הרצפה נקבעת
בשקע (`antimode_floor` ב-`build_dataset.py`).

**סטיית תקן נפסלה כשיטת סינון, ואין לחזור אליה.** היא נותנת לבדיקת קבצים
רצפה של 4.6 ימים ומשאירה גיוסים תוך שבוע. המשתמש קבע שזה בלתי אפשרי
בתהליך הזה, והדרישה גוברת על מה שסטטיסטית "מסתדר". סטיית התקן עדיין
מדווחת ב-`sigma_floor_days` ובאקסל, לצורך השוואה בלבד.

**תאריך יעד משנה כמויות, לא רק תאריכים.** בכל מצב שבו יש יעד, הדרישה
מחושבת לפי `effective_rate` = שיעור הגיוס כפול `curve_share` של הזמן
שנותר. אין מסלול שמחשב דרישה בלי להתחשב בזמן כשניתן תאריך - זו היתה
תקלה שדווחה: «400 תוך חודש» החזיר 5,205 בדיקות קבצים, כמות שמניבה
כ-13 גיוסים בחודש.

**יש עוזר אחד לחישוב הדרישה: `_requirement_rows`.** `required_plan`
ו-`gap_plan` שניהם קוראים לו. בעבר היו שני מימושים נפרדים ורק אחד
תוקן, וכך נולדה התקלה. `test_both_plan_paths_agree` אוכף שהם לא
יתפצלו שוב. אין לשכפל את החישוב הזה.

**מספר בלתי אפשרי לא מוצג כתשובה, אבל רק כשיש חלון זמן.** שלב שאינו
יכול לספק בזמן מקבל `required: null`. דרישה שגדולה מ-`capacity`
(=`observed_per_day * days`, כמה מועמדים עוברים בשלב בתוך החלון לפי
הקצב הנמדד) מסומנת `feasible: false`, והתצוגה מראה «לא בזמן הזה»
במקום המספר.

**בלי תאריך יעד אין תקרה בכלל.** `capacity` מחזיר `None`, וכל דרישה
היא `feasible`. זו היתה תקלה שדווחה: יעד של 4,000 גיוסים - סביר
לגמרי, הארגון מגייס כ-3,700 בשנה - הוחזר כ«לא בר-השגה» בכל שלב,
מפני שכל דרישה הושוותה לכמות שנמדדה ב-229 ימים בלבד. **אין להשוות
דרישה בלי מועד לכמות שנמדדה בחלון זמן.** במקום פסילה, `pace_days`
אומר את המחיר האמיתי: כמה זמן לוקח לצבור את הכמות בקצב הנמדד
(124,778 הגשות = כ-2.2 שנים).

ההגנה שבגללה הכלל נולד נשמרת במלואה - «400 תוך חודש» עדיין דורש
152,470 בדיקות קבצים ועדיין נפסל, כי שם יש תאריך ולכן יש תקרה.
`test_the_capacity_guard_still_catches_the_reported_failure` נועל
את זה, ו-`test_an_open_target_has_no_ceiling_at_all` נועל את הצד השני.

**`observed` הוא הספירה האמיתית בקובץ, לא גודל הקוהורט.** קודם הוא
נלקח מ-`basis` (23,786 להגשות במקום 35,711), כלומר אמת המידה היתה
קטנה פי כמה מהמציאות. עכשיו `stage["observed"]` נבנה ישירות
ב-`build_dataset` עם `candidates`, `days` ו-`per_day`.

**«כמה צריך שיעמדו שם» לעולם אינו נפסל.** הוא אינו מוגבל בזמן מעצם
הגדרתו, ולכן `required_by_feasible` תמיד אמת ומלווה אותו
`required_by_pace_days` בלבד.

**המשפך נפתח בשלב שממנו היעד אפשרי** (`feasible_stages`), ולא
בשלב הראשון תמיד. אין טעם לפתוח במשפך שמוביל לכמות שמעולם לא היתה.

**כשיש תאריך יעד, שורת הגיוס במשפך מציגה `hires_in_time`.** הכמות
הנדרשת מחושבת לפי הזמן, אבל `project_cohort` גוזר קדימה בלי הגבלת
זמן - ולכן `hires` הוא מספר אחר מהיעד. הצגת `hires` שם הראתה
11,716 ליעד של 400. אין להחליף בחזרה.

**לבדוק את המסך, לא רק את המנוע.** שתי התקלות האלה עברו את כל
בדיקות המנוע ונתפסו רק בהרצת קוד הממשק על התרחיש האמיתי. כשמשנים
לוגיקה שמשפיעה על תצוגה - להריץ את `ui_smoke.js` ולהסתכל בפלט.

**יעד יחד עם שלבים = כמה *עוד* צריך.** אם הוזן יעד וגם מלאי קיים,
המספרים חייבים לנכות את מי שכבר בתהליך (`gap_plan`). דרישה שמתעלמת
מהמלאי מנופחת ומטעה. עם תאריך יעד, השיעור שבשימוש הוא שיעור הגיוס
כפול `curve_share` של הזמן שנותר, ושלב שאינו יכול לספק בזמן מקבל
`null` ולא מספר.

**במצב יעד, המשפך הרציף מוצג לפני «כל שלב לבדו».** המשתמש העיר
שהסדר ההפוך מטעה - הוא נראה כאילו צריך 5,205 בדיקות קבצים *וגם*
516 ביחב"מ, בעוד שמדובר באותם אנשים בזמנים שונים. אין להחזיר את
הסדר.

**דרישה מקצועית: אין גיוס תוך שבוע מבדיקת קבצים.**
`test_no_hire_within_a_week_of_a_file_check` נועל את זה. אם קובץ מקור עתידי
יחזיר גיוסים כאלה, הבדיקה תיפול ותאלץ התייחסות. אין להחליש אותה - אפשר
לכוונן את `spike_ratio` או לקבוע רצפה ידנית ב-`min_days`.

**התפלגות חלקה אינה נחתכת.** שלוש הגנות מונעות זיהוי שווא: החיפוש נעצר
בחציון, הבליטה גדולה פי `spike_ratio` מהשקע, וגם מה שאחרי השקע גדול פי
`spike_ratio` ממנו. בלעדיהן נחתכים מעברים מהירים אמיתיים כמו יחב"מ אל
הגיוס. אין להסיר אף אחת מהשלוש.

**כל מעבר נמדד ישירות בין שני השלבים.** לא מכפלה של אחוזי מעבר ולא יחס של
יחסים. המשפך אינו סדרתי - מתוך בדיקת קבצים מגיעים לגיוס 8.4% אך ליחב"מ רק
8.3% - ולכן שרשור אחוזים היה מייצר מספרים שגויים.

**כל תוצאה היא מספר יחיד - ממוצע שני בסיסי המדידה.** הנתונים קטועים מימין:
מי שביצע פעילות סמוך לסוף התקופה עדיין לא הספיק להתקדם. לכן כל שיעור נמדד
פעמיים - בסיס אחד על חלון זמן קבוע, ובסיס שני על קוהורט בשל - והערך שבשימוש
הוא הממוצע ביניהם.

שני הבסיסים נשמרים תמיד ב-`data/recruitment_data.json`, מוצגים בטבלת הבסיס
שבעמוד ובדוח `make verify`. המשתמש ביקש במפורש לא לראות טווחים בתצוגה, ולכן
אין להחזיר טווח משום פונקציה במנוע. אין למחוק את שני הבסיסים מהנתונים -
בלעדיהם אי אפשר לחשב את הממוצע ואי אפשר להסביר מאיפה הוא בא.

**כל מספר בתצוגה אומר ממה הוא נגזר.** מתחת לכל מספר בגרף כתוב במפורש מאיזו
קבוצה ומאיזה שלב הוא בא. זו דרישה מפורשת של המשתמש, ו-`ui_smoke.js` אוכף
שכל שורה במשפך נושאת מקור.

**כמה שלבים שהוזנו = כמה קבוצות נפרדות.** הסכום מניח שאין חפיפה, והמערכת
מציגה על כך אזהרה מפורשת. אין לנחש אם יש חפיפה.


**האפיון מתעדכן עם הקוד.** כל שינוי בפונקציונליות מתועד מיד ב-
`אפיון המערכת.txt`.

**לא לפרסם מזהי מועמדים.** קובצי המקור מכילים מספרי מועמד. הם נשארים
בתיקיית «נתונים למערכת» ואינם נכנסים לעמוד הבנוי, שמתפרסם באופן ציבורי.
המחשבון מכיל אך ורק נתונים מצרפיים.

### פקודות

```bash
make          # בניית הנתונים + שני עמודי המחשבון + קובצי האקסל
make excel    # רק קובצי האקסל
make test     # 154 בדיקות
make verify   # דוח בדיקה של קובצי המקור
make basis    # יחסי הגיוס וזמני הגיוס בשורת פקודה
make plan T=400 BY=2026-12-31   # הלוח של מנהלת הגיוס
```
