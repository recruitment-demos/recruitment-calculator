# הנחיות עבודה בפרויקט הזה

## זרימת העבודה שהמשתמש הגדיר

### 1. תיקיית «תשובות»

**כל תשובה שאני נותן נשמרת כקובץ טקסט בתיקיית `תשובות/`** - בלי יוצא מן הכלל,
גם כשמדובר בתשובה קצרה או בשאלת הבהרה.

שם הקובץ: `NNN - YYYY-MM-DD - <תיאור קצר>.txt`
כאשר NNN הוא מספר רץ תלת-ספרתי לפי סדר כרונולוגי.

לפני כתיבת קובץ חדש: לבדוק את המספר האחרון בתיקייה ולהמשיך ממנו.
לא לדרוס קבצים קיימים.

התוכן הוא התשובה עצמה בטקסט רגיל, קריא בלי עיצוב מיוחד.

### 2. תיקיית «שינויים במערכת»

- `שינויים במערכת/דברים לשנות.txt` - המשתמש כותב כאן מה לשנות.
- `שינויים במערכת/היה/` - ארכיון הבקשות שכבר בוצעו.

**כשהמשתמש אומר "רוץ"** (או כל וריאציה: "תרוץ", "בצע", "לך"):

1. לקרוא את `דברים לשנות.txt`.
2. להתעלם משורות שמתחילות ב-`#`.
3. אם משהו לא ברור או שיש לו כמה פירושים - **לשאול לפני שנוגעים בקוד**.
4. לבצע את השינויים.
5. להריץ `make test` ולוודא שהכל עובר. אם משנים נתונים - גם `make`.
6. להעביר את הקובץ ל-`היה/` בשם `YYYY-MM-DD - NNN - <תיאור>.txt`,
   עם סיכום קצר בראשו: מה התבקש, מה בוצע, ומה לא בוצע ולמה.
7. ליצור `דברים לשנות.txt` **ריק לחלוטין** - בלי כותרת, בלי הסבר,
   בלי תבנית. קובץ באפס בתים. זו בקשה מפורשת של המשתמש.
8. לשמור את התשובה ב-`תשובות/`.

אם בקשה מסוימת חוסמת או בעייתית - לבצע את כל השאר במלואו, ולומר במפורש
מה הושאר ולמה. לא לצמצם את ההיקף בשקט.

---

## כללי הפרויקט

**מספרי גיוס לעולם לא נכתבים ידנית בקוד.** כל יחס המרה, זמן גיוס והתפלגות
נשאבים מקובצי האקסל שבתיקיית «נתונים למערכת» דרך `tools/build_dataset.py`.
אם ערך צריך להשתנות - מחליפים את קובץ המקור ומריצים `make`. אין לערוך ידנית
את `data/recruitment_data.json`, את `index.html` או את `מחשבון גיוס.html` -
שלושתם נוצרים אוטומטית.

**עורכים את `web/template.html`, לא את קובצי הפלט.**
`index.html` הוא מה ש-GitHub Pages מגיש, ו-`מחשבון גיוס.html` הוא אותו תוכן
בדיוק כקובץ עצמאי לשליחה. שניהם תוצרי בנייה.

**שני מנועי חישוב חייבים להישאר מסונכרנים.** יש לוגיקת חישוב גם ב-
`recruit_calc/engine.py` וגם ב-JS שבתוך `web/template.html`, בין הסימנים
`__ENGINE_START__` ו-`__ENGINE_END__`. כל שינוי בלוגיקה חייב להיעשות בשניהם,
ו-`tests/test_parity.py` מוודא שהם מחזירים אותו דבר על מאות תרחישים.

**לא לנחש נתונים שאינם במקור.** לשלב «הגשות» אין מקור נתונים בקבצים שהתקבלו,
ולכן הוא מסומן `has_data: false`, לא מקבל יחס המרה, ואינו משתתף בחישוב.
אם יתקבל קובץ הגשות - מוסיפים אותו ל-`config/params.json` ומריצים `make`.

**כל תוצאה היא טווח, לא מספר יחיד.** הנתונים קטועים מימין: מי שביצע פעילות
סמוך לסוף התקופה עדיין לא הספיק להתגייס. לכן כל יחס המרה נמדד פעמיים -
גבול תחתון על חלון קבוע, וגבול עליון על קוהורט בשל בלבד - והמחשבון מציג את
הטווח. אין להחליף את הטווח במספר אחד "נוח יותר".

**האפיון מתעדכן עם הקוד.** כל שינוי בפונקציונליות מתועד מיד ב-
`אפיון המערכת.txt`.

**לא לפרסם מזהי מועמדים.** קובצי המקור מכילים מספרי מועמד. הם נשארים
בתיקיית «נתונים למערכת» ואינם נכנסים לעמוד הבנוי, שמתפרסם באופן ציבורי.
המחשבון מכיל אך ורק נתונים מצרפיים.

### פקודות

```bash
make          # בניית הנתונים + שני עמודי המחשבון
make test     # 35 בדיקות
make verify   # דוח בדיקה של קובצי המקור
make basis    # יחסי הגיוס וזמני הגיוס בשורת פקודה
```
